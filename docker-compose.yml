version: "3.9"

services:
  keycloak:
    image: quay.io/keycloak/keycloak
    environment:
      DB_VENDOR: MYSQL
      DB_ADDR: mysql
      DB_PORT: 3306
      DB_DATABASE: keycloak
      DB_USER: keycloak
      DB_PASSWORD: password
    ports:
      - 8080:8080
      - 8443:8443
    depends_on:
      - mysql
    networks:
      - app-network

  mysql:
    image: mysql:latest
    environment:
      MYSQL_DATABASE: keycloak
      MYSQL_USER: keycloak
      MYSQL_PASSWORD: password
      MYSQL_ROOT_PASSWORD: root
    ports:
      - 3306:3306
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - app-network

  node_app:
    build: .
    ports:
      - 4000:3000
    environment:
      POST: 4000
      DATABASE_URL: mysql://keycloak:password@mysql:3306/keycloak
      KEYCLOAK_URL: http://keycloak:8080/auth
      KEYCLOAK_REALM: realm1
      KEYCLOAK_CLIENT_ID: node-app
      KEYCLOAK_CLIENT_SECRET: c83a424a-0d7e-4e15-8b2e-89d87f05ab20
    volumes:
      - ./app:/app
    working_dir: /app
    command: ["npm", "start"]
    depends_on:
      - keycloak
      - mysql
    networks:
      - app-network

volumes:
  mysql_data:

networks:
  app-network:
